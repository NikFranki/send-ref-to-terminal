{
  "name": "send-ref-to-terminal",
  "displayName": "Send File Reference to Terminal",
  "description": "Send selected code's file reference (path:startLine-endLine) to the active terminal",
  "version": "0.0.4",
  "publisher": "Franki",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/NikFranki/send-ref-to-terminal"
  },
  "keywords": [
    "terminal",
    "file reference",
    "claude",
    "code reference"
  ],
  "engines": {
    "vscode": "^1.90.0"
  },
  "icon": "icon.png",
  "categories": [
    "Other"
  ],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sendRefToTerminal.send",
        "title": "Add to Terminal",
        "shortTitle": "Add to Terminal"
      },
      {
        "command": "sendRefToTerminal.sendFile",
        "title": "Send File to Terminal"
      }
    ],
    "keybindings": [
      {
        "command": "sendRefToTerminal.send",
        "key": "ctrl+shift+alt+r",
        "when": "editorTextFocus && editorHasSelection"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "sendRefToTerminal.send",
          "when": "editorHasSelection",
          "group": "navigation@1"
        }
      ],
      "explorer/context": [
        {
          "command": "sendRefToTerminal.sendFile",
          "when": "!explorerResourceIsFolder",
          "group": "navigation@1"
        }
      ],
      "editor/title/context": [
        {
          "command": "sendRefToTerminal.sendFile",
          "group": "navigation@1"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.0.0"
  }
}
